# Models Directory

This folder is reserved for saved model artifacts when you choose to persist trained models.

The current project trains models at runtime in the notebook and Streamlit dashboard so the repository stays lightweight and reproducible.

